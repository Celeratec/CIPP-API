# Upstream Sync Checkpoint — 2026-08-14

Cycle type: **Major** (upstream **10.7.5 → 10.8.5**; plan scoped as 10.8.4, tip advanced).

| Repo | From | To | Upstream tip |
|---|---|---|---|
| CIPP | 10.7.5 (`274880fd0`) | **10.8.5** | `upstream/main` @ `a1b620403` |
| CIPP-API | 10.7.5 (`245d9d35b`) | **10.8.5** | `upstream/master` @ `e02ac3724` |

Branches: `manage365/upstream-sync-cipp-20260814` /
`manage365/upstream-sync-cipp-api-20260814`.
Backup tags: `backup/pre-upstream-sync-cipp-20260814` /
`backup/pre-upstream-sync-cipp-api-20260814`.

Delta: [UPSTREAM_DELTA_CIPP_20260814.md](./UPSTREAM_DELTA_CIPP_20260814.md),
API `docs/upstream-sync/UPSTREAM_DELTA_CIPP_API_20260814.md`.
Baselines: [BASELINES_INTAKE_20260814.md](./BASELINES_INTAKE_20260814.md).

## Applied

| Batch | Theme | Outcome |
|---|---|---|
| 1 | ConversionTable, ListSecureScoreReport + Get-CIPPSecureScoreReport, FE securescore + AllTenants helpers | **Apply / Adapt** |
| 2 | ListIntuneApprovalRequests + alert + retry; BitLocker search page + nested ExecBitlockerSearch; FE MAA + BitLocker nav | **Apply** |
| 3 | ListRoles (nested), ListTenantAllowBlockList (Spamfilter), TABL helpers, ExecSetDefaultMFAMethod, CA/EXO PowerShell/DisableM365GroupUsers standards | **Apply** |
| 4 | Baselines modules (29) + Set-CippBaselineRunContext + orch + activities + 9 HTTP + BaselineStandards (81) + FeatureFlags/Timers (flag **off**) + FE pages/components; nav commented | **Feature intake** |
| 5 | ListBrandingSettings/Presets + Get-CIPPBranding*; ListCommunityRepoTemplates + catalog/CippTemplateCatalog (nav not fully rewired) | **Apply (partial)** |
| — | M365Licenses.json | **Apply** |

## Skipped / Deferred

| Item | Outcome | Why |
|---|---|---|
| SSO / authentication nav / container-management / ForcedSsoMigration | **Skip** | SWA/Craft |
| Node/jsdom engine bumps, wholesale yarn.lock | **Defer** | After ports settle |
| Full BEC / user detail rewrite; Default MFA **UI** wire into fork user page | **Defer Adapt** | API ported; FE surgical later |
| Full community-repos index.js rework | **Defer Adapt** | Catalog API + page present |
| Full branding settings JSX replace | **Defer Adapt** | List APIs ported; keep Manage365 branding UI |
| CVE report rewrite | **Skip** | Fork removed CVE surface on origin |
| Teams V2 | Unchanged deferred | |
| PublicMcpRegister | **Defer** | MCP already intaken separately |

## Verification

- PowerShell AST: Baselines/*.ps1 + key new HTTP entrypoints OK
- `Baselines.Enabled` = **false** in FeatureFlags
- SAMManifest: no new permission IDs this cycle

## Versions

- Manage365 **5.32.2 → 5.33.0**; upstream baseline **10.7.5 → 10.8.5**
- API `version_latest.txt` / `Config/version_latest.txt` → **10.8.5**

## Post-deploy

1. Deploy SWA + **all** Function slots (esp. standards processor for baseline timer when enabled)
2. Smoke: MAA Requests, BitLocker search, Secure Score, `/tenant/baselines` (direct URL)
3. Enable `Baselines` flag only after orchestrator smoke
4. Optional: uncomment Baselines nav in `src/layouts/config.js`

## Next cycle base SHAs

| Repo | Base |
|---|---|
| CIPP | `a1b620403` (upstream/main) |
| CIPP-API | `e02ac3724` (upstream/master) |
